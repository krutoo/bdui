# Example of using `@krutoo/bdui`

To run server:

```sh
go run .
```

And then to run frontend you need to run `examples/basics` but without server part (you can comment or remove server config from `rspack.config.ts`).
